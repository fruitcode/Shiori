<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Shiori">


    <Style TargetType="{x:Type local:TimeLine}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TimeLine}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="14"/>
                        </Grid.RowDefinitions>
                        <Rectangle Grid.Row="1" Fill="Transparent"/>
                        <Rectangle Grid.Row="1" Fill="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Width="{Binding BarWidth, RelativeSource={RelativeSource TemplatedParent}}" HorizontalAlignment="Left"/>
                        <Border Grid.Row="1" BorderThickness="1 1 0 0" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                        <Border Grid.Row="1" BorderThickness="0 0 1 1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"/>
                    </Grid>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
